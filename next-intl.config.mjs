export default {
  // Locales available in `messages/*.json`
  locales: ["en", "am", "fr", "es", "de", "om"],
  // Default locale for the app
  defaultLocale: "en",
  // Enable or disable automatic locale detection
  localeDetection: true,
}
